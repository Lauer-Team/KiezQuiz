/* KiezQuiz — shared app header lives in index.html (#app-header); hub/city share one bar. */
(function () {
  window.kiezGlobalHeader = {
    renderHubHeader: function () {
      return '';
    }
  };
})();
