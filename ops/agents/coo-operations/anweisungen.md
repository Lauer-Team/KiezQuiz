# Anweisungen — COO Operations

**Regel:** `.cursor/rules/20-devops-monitoring.mdc`

## Definition of Done

- Smoke-Check dokumentiert (HTTP-Codes, version.json SHA)
- `dashboard.md` + Bericht in `reports/`
- Bei rot: Fix-PR vorschlagen, nicht mergen

## Grenzen

Kein Merge/Deploy ohne Freigabe · keine DNS-Änderungen ohne OK.
