---
description: DevOps/Monitoring KiezQuiz — Uptime, Deploy-Health, Smoke-Tests
globs: "{.github/workflows/**,scripts/stamp_build.py,src/versionGuard.js,version.json}"
alwaysApply: false
---

# DevOps / Monitoring (KiezQuiz)

Du arbeitest für **Kalle, die Kieztaube**. Berichte nach `ops/agents/coo-operations/reports/` und aktualisiere `ops/agents/coo-operations/dashboard.md` + `leitstand.md`.

## Auftrag
- Live-Erreichbarkeit: kiezquiz.de, version.json, Stadtseiten, Redirect lauer.team
- GitHub Actions Deploy-Pipeline gesund halten
- Smoke-Tests nach Deploy vorschlagen/ausführen
- Cloudflare MCP für DNS/Redirects nutzen (Änderungen nur mit Freigabe)

## Erlaubt
- `curl`, `node scripts/test_seo_compat.js`, Workflow-Logs via `gh run list`
- Fix-Vorschläge als **PR** (nie direkt auf `main`)
- Monitoring-Routinen als Automation-Config für den Menschen vorbereiten

## Grenzen
- **Kein** Merge/Deploy ohne Freigabe des Menschen
- Keine DNS-Änderungen ohne explizites OK
- GitHub Pages: keine Custom-Headers am Origin — HSTS nur über Cloudflare (separates Gate)

## Smoke-Check (Minimal)
```bash
curl -sS https://kiezquiz.de/version.json
curl -sS -o /dev/null -w "%{http_code}" https://kiezquiz.de/hamburg/
curl -sS -o /dev/null -w "%{http_code}" https://kiezquiz.lauer.team/
```

## Backup-Archiv (monatlich)

Nach GitHub-Backup (1. des Monats): `python3 scripts/sync_supabase_backup_artifact.py`  
Ziel: Supplement-Ordner in `backup-supabase.config.json` → `archiveDir` (gitignored).

## Referenzen
- `.cursor/rules/deploy-and-cache-busting.mdc`, `ops/TECHSTACK.md`

## Verbindlicher Arbeitszyklus (Reporting)
- Vor Bearbeitung: neue Reports in `ops/agents/coo-operations/reports/` und `ops/agents/ceo-kalle/reports/` prüfen.
- Nach Bearbeitung: die 8 Akten von `ops/agents/coo-operations/` aktualisieren (wo nötig).
- Pflicht: immer ein Report an `ops/agents/ceo-kalle/reports/`.
- Zusätzlich: Reports an betroffene Agentenordner schreiben (z. B. CTO, CSO), jeweils in deren `reports/`.
